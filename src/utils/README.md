# utils

Shared utility helpers used across the frontend for formatting and conversions.

## Files

| File | Description |
|------|-------------|
| `timezoneUtils.js` | UTC-based date/time helpers for converting between datetime-local input strings and UTC ISO strings, formatting display values, and computing default future timestamps. |
