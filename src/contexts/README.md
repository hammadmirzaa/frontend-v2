# contexts

React context providers that expose application-wide state and side effects to the component tree.

## Files

| File | Description |
|------|-------------|
| `AuthContext.jsx` | Authentication context providing the current user, login, signup, and logout actions, JWT persistence in localStorage, and automatic redirect handling for inactive accounts. |
